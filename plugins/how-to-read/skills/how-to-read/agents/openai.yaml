interface:
  display_name: "How to Read"
  short_description: "Make every response easier to read"
  default_prompt: "Use $how-to-read to answer first and make every response easy to scan."

policy:
  allow_implicit_invocation: true
